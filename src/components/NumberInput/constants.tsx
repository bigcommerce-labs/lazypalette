export const enum Status {
    Invalid = 'Invalid',
    Undefined = 'Undefined',
}

export const enum Notes {
    Required = 'Required*',
}

export const enum Sign {
    Minus = '-',
    Plus = '+',
}
