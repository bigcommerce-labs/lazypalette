import styled from 'styled-components';

export const Wrapper = styled.div`
    margin: 0;
`;

export const Publish = styled.div`
    vertical-align: top;
    float: right;
    padding-right: 1rem;
`;
